# Transaction Analyzer

[![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)](https://www.python.org/)
[![Poetry](https://img.shields.io/badge/Poetry-Latest-60A5FA.svg)](https://python-poetry.org/)
[![Code style: black](https://img.shields.io/badge/code%20style-black-000000.svg)](https://github.com/psf/black)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

Приложение для анализа транзакций из Excel-файла. Генерирует JSON-данные для веб-страниц, формирует Excel-отчеты и предоставляет различные сервисы для работы с транзакциями.

## 🚀 Основные возможности

- 📊 **Анализ транзакций** - обработка данных из Excel-файлов
- 🌐 **Веб-страницы** - генерация JSON для главной страницы и страницы событий
- 🔍 **Сервисы поиска** - поиск транзакций по различным критериям
- 📈 **Отчеты** - автоматическая генерация отчетов с сохранением в файлы
- 🔐 **Безопасность** - защита конфиденциальных данных и API ключей

## 📋 Описание проекта

Проект предназначен для:
- Анализа транзакций из Excel-файлов
- Генерации JSON-данных для веб-страниц
- Формирования Excel-отчетов
- Предоставления сервисов для поиска и анализа транзакций

### Источник данных

Данные о транзакциях находятся в [Google Sheets](https://docs.google.com/spreadsheets/d/1yXnr282zAMcTkEhIwZFaJlPJvAeZIwvB/edit?usp=sharing&ouid=106765172812768673646&rtpof=true&sd=true).

Для работы с проектом необходимо:
1. Экспортировать данные из Google Sheets в формат Excel (`.xlsx`)
2. Сохранить файл в папку `data/` проекта
3. Указать путь к файлу в переменной окружения `EXCEL_FILE` или передать его в функции

### Структура данных транзакций

Excel-файл содержит следующие колонки:

| Колонка | Название | Описание | Тип данных |
|---------|----------|----------|------------|
| A | Дата операции | Дата, когда произошла транзакция (формат: `DD.MM.YYYY HH:MM:SS`) | str |
| B | Дата платежа | Дата, когда был произведен платеж (формат: `DD.MM.YYYY`) | str |
| C | Номер карты | Последние 4 цифры номера карты (например, `*7197`) | str |
| D | Статус | Статус операции (например, `OK`, `FAILED`) | str |
| E | Сумма операции | Сумма транзакции в оригинальной валюте (отрицательная для расходов) | float |
| F | Валюта операции | Валюта, в которой была произведена транзакция (например, `RUB`) | str |
| G | Сумма платежа | Сумма транзакции в валюте счета | float |
| H | Валюта платежа | Валюта счета | str |
| I | Кэшбэк | Размер полученного кешбэка | float |
| J | Категория | Категория транзакции | str |
| K | MCC | Код категории транзакции (соответствует международной классификации) | str |
| L | Описание | Описание транзакции | str |
| M | Бонусы (включая кэшбэк) | Количество полученных бонусов (включая кешбэк) | float |
| N | Округление на инвесткопилку | Сумма, которая была округлена и переведена на «Инвесткопилку» | float |
| O | Сумма операции с округлением | Сумма транзакции, округленная до ближайшего целого числа | float |

**Примеры категорий транзакций:**
- Супермаркеты
- Переводы
- Каршеринг
- Пополнения
- Канцтовары
- Ж/д билеты
- Фастфуд
- Дом и ремонт
- Топливо
- Транспорт
- Связь
- Услуги банка
- Бонусы
- ЖКХ
- И другие

## 🚀 Быстрый старт

### Требования

- Python 3.9 или выше
- Poetry (менеджер зависимостей)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd transaction-analyzer
```

2. Установите зависимости через Poetry:
```bash
poetry install
```

3. Активируйте виртуальное окружение:
```bash
poetry shell
```

4. Создайте файл `.env` на основе шаблона `.env.example`:
```bash
cp .env.example .env
```

5. Заполните переменные окружения в файле `.env`:
```env
API_KEY=your_api_key_here
API_URL=https://api.example.com
```

### Подготовка данных

1. Экспортируйте данные из [Google Sheets](https://docs.google.com/spreadsheets/d/1yXnr282zAMcTkEhIwZFaJlPJvAeZIwvB/edit?usp=sharing&ouid=106765172812768673646&rtpof=true&sd=true):
   - Откройте таблицу
   - Файл → Скачать → Microsoft Excel (`.xlsx`)
   - Сохраните файл в папку `data/` проекта

2. Убедитесь, что файл доступен:
```bash
ls data/operations.xlsx  # файл должен называться operations.xlsx
```

**⚠️ Безопасность:** Файл `data/operations.xlsx` содержит данные транзакций и автоматически игнорируется Git (папка `data/` в `.gitignore`). Не добавляйте этот файл в репозиторий вручную.

3. Создайте файл `user_settings.json` в корне проекта (уже создан):
```json
{
  "user_currencies": ["USD", "EUR"],
  "user_stocks": ["AAPL", "AMZN", "GOOGL", "MSFT", "TSLA"]
}
```

### Запуск

Запустите главный модуль приложения:
```bash
poetry run python -m src.main
```

## 📁 Структура проекта

```
transaction-analyzer/
├── src/                    # Исходный код приложения
│   ├── __init__.py
│   ├── main.py            # Главный модуль для запуска
│   ├── views.py           # Функции для веб-страниц
│   ├── services.py        # Сервисы для работы с транзакциями
│   ├── reports.py         # Функции для генерации отчетов
│   ├── utils.py           # Вспомогательные функции
│   └── logger_config.py   # Конфигурация логирования
├── tests/                  # Тесты
│   ├── __init__.py
│   ├── test_views.py
│   ├── test_services.py
│   └── test_reports.py
├── data/                   # Данные (Excel-файлы, JSON)
│   └── operations.xlsx    # Файл с транзакциями (экспортировать из Google Sheets)
├── user_settings.json      # Пользовательские настройки (валюты и акции)
├── .env                    # Переменные окружения (не коммитится)
├── .env.example            # Шаблон переменных окружения
├── .flake8                 # Конфигурация Flake8
├── .gitignore              # Игнорируемые файлы
├── pyproject.toml          # Конфигурация Poetry и инструментов
├── poetry.lock             # Зафиксированные версии зависимостей
└── README.md               # Документация проекта
```

## 🏷️ Теги задач

При работе над задачами используются следующие теги:

- `#json` - работа с JSON-данными
- `#requests` - HTTP-запросы к API
- `#API` - работа с внешними API
- `#datetime` - работа с датами и временем
- `#logging` - логирование операций
- `#pytest` - тестирование
- `#pandas` - работа с данными

## 📋 Категории задач

### Веб-страницы
- **Главная** - генерация JSON-ответа для главной страницы
- **События** - генерация JSON-ответа для страницы событий

### Сервисы
- **Выгодные категории повышенного кешбэка** - анализ категорий с повышенным кешбэком
- **Инвесткопилка** - расчет накоплений для инвестиций
- **Простой поиск** - поиск транзакций по запросу
- **Поиск по телефонным номерам** - поиск транзакций с телефонными номерами
- **Поиск переводов физическим лицам** - поиск переводов физлицам

### Отчеты
- **Траты по категории** - отчет о тратах по категории за период
- **Траты по дням недели** - анализ трат по дням недели
- **Траты в рабочий/выходной день** - сравнение трат в рабочие и выходные дни

## 🎯 Принципы разработки

### Разделение бизнес-логики от получения данных

**Важно:** Не передавайте имя Excel-файла в функцию, которая реализует бизнес-логику. Вместо этого:

1. Считывайте данные в отдельной функции
2. Передавайте уже обработанные данные в основную функцию

**Преимущества:**
- Легко менять способ получения данных (Excel, API, БД)
- Упрощает тестирование (можно передавать тестовые данные напрямую)
- Разделение ответственности

**Пример:**
```python
# ✅ Хорошо: разделение получения данных и бизнес-логики
def load_transactions(file_path: str) -> List[Dict[str, Any]]:
    """Загружает транзакции из Excel."""
    # Чтение данных
    ...

def process_transactions(transactions: List[Dict[str, Any]]) -> Dict[str, Any]:
    """Обрабатывает транзакции (бизнес-логика)."""
    # Обработка данных
    ...

# ❌ Плохо: смешивание получения данных и бизнес-логики
def process_transactions(file_path: str) -> Dict[str, Any]:
    """Обрабатывает транзакции."""
    # Чтение + обработка в одной функции
    ...
```

### Диапазон данных для анализа

Данные для анализа и вывода на веб-страницах — это данные **с начала месяца, на который выпадает входящая дата, по входящую дату**.

**Пример:**
- Если дата — `20.05.2020`, то данные для анализа будут в диапазоне `01.05.2020 - 20.05.2020`

### Пользовательские настройки

Валюты и акции для отображения на веб-страницах задаются в файле `user_settings.json`:

```json
{
  "user_currencies": ["USD", "EUR"],
  "user_stocks": ["AAPL", "AMZN", "GOOGL", "MSFT", "TSLA"]
}
```

**Примечание:** Для получения данных о валютах и ценах на акции используйте библиотеку `requests`. Сервисы, предоставляющие API для курса валют и цен на акции, подберите самостоятельно.

### Как JSON-ответ используется веб-страницей

Веб-страница обычно связана с бэкендом через API. Фронтенд обращается к API бэкенда с запросом на получение данных, на что бэкенд отвечает JSON-ответом. Затем фронтенд подставляет значения из JSON-ответа на веб-страницу, чтобы пользователь мог увидеть их на экране.

**В данном случае:**
- Бэкенд предоставляет JSON-ответ с данными о расходах за месяц, среднем размере транзакции, сумме кешбэка, топе-3 категорий расходов и топе-3 транзакций
- Фронтенд использует этот ответ, чтобы отобразить соответствующие значения на веб-странице
- Бэкенд может использовать сторонние API для получения текущих цен на валюты и акции S&P500
- Фронтенд использует эти данные, чтобы отобразить соответствующие значения на веб-странице

## 🔧 Функциональность

### Веб-страницы

#### Страница "Главная"
- **Модуль**: `src/views.py`
- **Функция**: `home_page(date_time: str) -> dict`
- **Теги**: `#json`, `#requests`, `#API`, `#datetime`, `#logging`, `#pytest`, `#pandas`
- **Описание**: Генерирует JSON-данные для главной страницы на основе даты и времени
- **Входные данные**: Строка с датой и временем в формате `YYYY-MM-DD HH:MM:SS`
- **Используемые библиотеки**: `json`, `datetime`, `logging`, `pandas`, `requests` (API)

**Возвращаемый JSON содержит:**
- Приветствие ("Доброе утро" / "Добрый день" / "Добрый вечер" / "Доброй ночи")
- По каждой карте:
  - Последние 4 цифры карты
  - Общая сумма расходов
  - Кешбэк (1 рубль на каждые 100 рублей)
- Топ-5 транзакций по сумме платежа
- Курс валют (из `user_settings.json`)
- Стоимость акций из S&P500 (из `user_settings.json`)

**Диапазон данных:** С начала месяца по указанную дату

**Пример JSON-ответа:**
```json
{
  "greeting": "Добрый день",
  "cards": [
    {
      "last_digits": "5814",
      "total_spent": 1262.00,
      "cashback": 12.62
    },
    {
      "last_digits": "7512",
      "total_spent": 7.94,
      "cashback": 0.08
    }
  ],
  "top_transactions": [
    {
      "date": "21.12.2021",
      "amount": 1198.23,
      "category": "Переводы",
      "description": "Перевод Кредитная карта. ТП 10.2 RUR"
    },
    {
      "date": "20.12.2021",
      "amount": 829.00,
      "category": "Супермаркеты",
      "description": "Лента"
    }
  ],
  "currency_rates": [
    {
      "currency": "USD",
      "rate": 73.21
    },
    {
      "currency": "EUR",
      "rate": 87.08
    }
  ],
  "stock_prices": [
    {
      "stock": "AAPL",
      "price": 150.12
    },
    {
      "stock": "AMZN",
      "price": 3173.18
    }
  ]
}
```

#### Страница "События"
- **Модуль**: `src/views.py`
- **Функция**: `events_page(date: str, period: str = "M") -> dict`
- **Теги**: `#json`, `#requests`, `#API`, `#datetime`, `#logging`, `#pytest`, `#pandas`
- **Описание**: Генерирует JSON-данные для страницы событий
- **Входные данные**: 
  - `date`: Строка с датой
  - `period`: Диапазон данных (по умолчанию `"M"` - месяц)
    - `"W"` - неделя
    - `"M"` - месяц (с начала месяца по дату)
    - `"Y"` - год
    - `"ALL"` - все данные до указанной даты
- **Используемые библиотеки**: `json`, `datetime`, `logging`, `pandas`, `requests` (API)

**Возвращаемый JSON содержит:**
- **Расходы:**
  - Общая сумма расходов (округлено до целых)
  - Раздел "Основные": топ-7 категорий по тратам, остальные в "Остальное"
  - Раздел "Переводы и наличные": категории "Наличные" и "Переводы"
- **Поступления:**
  - Общая сумма поступлений (округлено до целых)
  - Раздел "Основные": поступления по категориям (отсортированы по убыванию)
- Курс валют (из `user_settings.json`)
- Стоимость акций из S&P500 (из `user_settings.json`)

**Пример JSON-ответа:**
```json
{
  "expenses": {
    "total_amount": 32101,
    "main": [
      {
        "category": "Супермаркеты",
        "amount": 17319
      },
      {
        "category": "Фастфуд",
        "amount": 3324
      },
      {
        "category": "Остальное",
        "amount": 2954
      }
    ],
    "transfers_and_cash": [
      {
        "category": "Наличные",
        "amount": 500
      },
      {
        "category": "Переводы",
        "amount": 200
      }
    ]
  },
  "income": {
    "total_amount": 54271,
    "main": [
      {
        "category": "Пополнение_BANK007",
        "amount": 33000
      },
      {
        "category": "Проценты_на_остаток",
        "amount": 1242
      }
    ]
  },
  "currency_rates": [
    {
      "currency": "USD",
      "rate": 73.21
    }
  ],
  "stock_prices": [
    {
      "stock": "AAPL",
      "price": 150.12
    }
  ]
}
```

### Сервисы

**Важно:** При реализации сервисов должны использоваться элементы функционального программирования.

#### Выгодные категории повышенного кешбэка
- **Модуль**: `src/services.py`
- **Функция**: `profitable_cashback_categories(data: List[Dict[str, Any]], year: int, month: int) -> Dict[str, float]`
- **Теги**: `#json`, `#datetime`, `#logging`, `#pytest`
- **Описание**: Анализирует, какие категории были наиболее выгодными для выбора в качестве категорий повышенного кешбэка
- **Входные параметры:**
  - `data` - данные с транзакциями (список словарей)
  - `year` - год, за который проводится анализ
  - `month` - месяц, за который проводится анализ
- **Выходные данные:** JSON с анализом, сколько на каждой категории можно заработать кешбэка в указанном месяце года
- **Используемые библиотеки**: `json`, `datetime`, `logging`

**Формат выходных данных:**
```json
{
    "Категория 1": 1000.0,
    "Категория 2": 2000.0,
    "Категория 3": 500.0
}
```

**Пример использования:**
```python
transactions = [
    {"Дата операции": "2021-12-15", "Категория": "Супермаркеты", "Кэшбэк": 5.0, ...},
    {"Дата операции": "2021-12-20", "Категория": "Фастфуд", "Кэшбэк": 2.0, ...}
]
result = profitable_cashback_categories(transactions, year=2021, month=12)
# {"Супермаркеты": 1000.0, "Фастфуд": 500.0, ...}
```

#### Инвесткопилка
- **Модуль**: `src/services.py`
- **Функция**: `investment_bank(month: str, transactions: List[Dict[str, Any]], limit: int) -> float`
- **Теги**: `#json`, `#datetime`, `#logging`, `#pytest`
- **Описание**: Позволяет копить через округление трат. Траты округляются до заданного предела, и разница попадает в «Инвесткопилку»
- **Входные параметры:**
  - `month` - месяц для расчета (строка в формате `'YYYY-MM'`, например `'2021-12'`)
  - `transactions` - список словарей с транзакциями, содержащими:
    - `Дата операции` - дата транзакции (строка в формате `'YYYY-MM-DD'`)
    - `Сумма операции` - сумма транзакции в оригинальной валюте (число)
  - `limit` - предел округления (целое число: 10, 50 или 100 ₽)
- **Выходные данные:** Сумма, которую удалось бы отложить в «Инвесткопилку» (float)
- **Используемые библиотеки**: `json`, `datetime`, `logging`

**Пример работы:**
- Покупка на 1712 ₽ с шагом округления 50 ₽
- Округляется до 1750 ₽
- В «Инвесткопилку» попадает 38 ₽

**Пример использования:**
```python
transactions = [
    {"Дата операции": "2021-12-15", "Сумма операции": 1712.0, ...},
    {"Дата операции": "2021-12-20", "Сумма операции": 234.5, ...}
]
result = investment_bank("2021-12", transactions, limit=50)
# Возвращает сумму округлений за месяц
```

#### Простой поиск
- **Модуль**: `src/services.py`
- **Функция**: `simple_search(query: str, transactions: List[Dict[str, Any]]) -> Dict[str, Any]`
- **Теги**: `#json`, `#logging`, `#pytest`
- **Описание**: Выполняет поиск транзакций по запросу. Ищет в описании и категории транзакций
- **Входные параметры:**
  - `query` - строка для поиска
  - `transactions` - список словарей с транзакциями
- **Выходные данные:** JSON-ответ со всеми транзакциями, содержащими запрос в описании или категории
- **Используемые библиотеки**: `json`, `logging`

**Пример использования:**
```python
transactions = [
    {"Описание": "Лента", "Категория": "Супермаркеты", ...},
    {"Описание": "Магнит", "Категория": "Супермаркеты", ...}
]
result = simple_search("Лента", transactions)
# Возвращает транзакции, содержащие "Лента" в описании или категории
```

#### Поиск по телефонным номерам
- **Модуль**: `src/services.py`
- **Функция**: `search_by_phone(transactions: List[Dict[str, Any]]) -> Dict[str, Any]`
- **Теги**: `#json`, `#logging`, `#pytest`, `#regex`
- **Описание**: Находит транзакции, содержащие в описании мобильные номера телефонов
- **Входные параметры:**
  - `transactions` - список словарей с транзакциями
- **Выходные данные:** JSON со всеми транзакциями, содержащими в описании мобильные номера
- **Используемые библиотеки**: `json`, `re`, `logging`

**Примеры описаний с номерами:**
- `Я МТС +7 921 11-22-33`
- `Тинькофф Мобайл +7 995 555-55-55`
- `МТС Mobile +7 981 333-44-55`

**Пример использования:**
```python
transactions = [
    {"Описание": "Я МТС +7 921 11-22-33", ...},
    {"Описание": "Пополнение счета", ...}
]
result = search_by_phone(transactions)
# Возвращает транзакции с телефонными номерами в описании
```

#### Поиск переводов физическим лицам
- **Модуль**: `src/services.py`
- **Функция**: `search_person_transfers(transactions: List[Dict[str, Any]]) -> Dict[str, Any]`
- **Теги**: `#json`, `#logging`, `#pytest`, `#regex`
- **Описание**: Находит транзакции, которые относятся к переводам физическим лицам
- **Входные параметры:**
  - `transactions` - список словарей с транзакциями
- **Выходные данные:** JSON со всеми транзакциями, которые относятся к переводам физлицам
- **Используемые библиотеки**: `json`, `re`, `logging`

**Критерии поиска:**
- Категория транзакции — `"Переводы"`
- В описании есть имя и первая буква фамилии с точкой

**Примеры описаний переводов физлицам:**
- `Валерий А.`
- `Сергей З.`
- `Артем П.`

**Пример использования:**
```python
transactions = [
    {"Категория": "Переводы", "Описание": "Валерий А.", ...},
    {"Категория": "Переводы", "Описание": "Перевод на карту", ...},
    {"Категория": "Супермаркеты", "Описание": "Лента", ...}
]
result = search_person_transfers(transactions)
# Возвращает только транзакции с категорией "Переводы" и описанием вида "Имя Ф."
```

### Отчеты

**Важно:** Все функции-отчеты должны использовать декоратор для автоматического сохранения результатов в файл.

#### Декоратор для отчетов

Создайте декоратор для функций-отчетов, который записывает в файл результат, который возвращает функция, формирующая отчет.

**Требования к декоратору:**
- Декоратор без параметра — записывает данные отчета в файл с названием по умолчанию (формат имени файла придумайте самостоятельно)
- Декоратор с параметром — принимает имя файла в качестве параметра

**Пример использования:**
```python
# Декоратор без параметра
@save_report
def spending_by_category(...):
    ...

# Декоратор с параметром
@save_report("custom_report.json")
def spending_by_category(...):
    ...
```

#### Траты по категории
- **Модуль**: `src/reports.py`
- **Функция**: `spending_by_category(transactions: pd.DataFrame, category: str, date: Optional[str] = None) -> pd.DataFrame`
- **Теги**: `#json`, `#pandas`, `#logging`, `#pytest`, `#datetime`
- **Описание**: Возвращает траты по заданной категории за последние три месяца (от переданной даты)
- **Входные параметры:**
  - `transactions` - датафрейм с транзакциями
  - `category` - название категории
  - `date` - опциональная дата (если не передана, берется текущая дата)
- **Выходные данные:** Датафрейм с тратами по заданной категории за последние три месяца
- **Используемые библиотеки**: `json`, `pandas`, `logging`, `datetime`

**Интерфейс функции:**
```python
def spending_by_category(transactions: pd.DataFrame,
                         category: str,
                         date: Optional[str] = None) -> pd.DataFrame:
    """
    Возвращает траты по заданной категории за последние три месяца.
    
    Args:
        transactions: Датафрейм с транзакциями
        category: Название категории
        date: Опциональная дата (формат: 'YYYY-MM-DD'). 
              Если не передана, используется текущая дата
    
    Returns:
        Датафрейм с тратами по категории за последние три месяца
    """
    pass
```

**Пример использования:**
```python
import pandas as pd
from src.reports import spending_by_category

df = pd.read_excel("data/operations.xlsx")
result = spending_by_category(df, category="Супермаркеты", date="2021-12-31")
# Возвращает траты по категории "Супермаркеты" за последние 3 месяца от 2021-12-31
```

#### Траты по дням недели
- **Модуль**: `src/reports.py`
- **Функция**: `spending_by_weekday(transactions: pd.DataFrame, date: Optional[str] = None) -> pd.DataFrame`
- **Теги**: `#json`, `#pandas`, `#logging`, `#pytest`, `#datetime`
- **Описание**: Возвращает средние траты в каждый из дней недели за последние три месяца (от переданной даты)
- **Входные параметры:**
  - `transactions` - датафрейм с транзакциями
  - `date` - опциональная дата (если не передана, берется текущая дата)
- **Выходные данные:** Датафрейм со средними тратами по дням недели за последние три месяца
- **Используемые библиотеки**: `json`, `pandas`, `logging`, `datetime`

**Интерфейс функции:**
```python
def spending_by_weekday(transactions: pd.DataFrame, 
                        date: Optional[str] = None) -> pd.DataFrame:
    """
    Возвращает средние траты в каждый из дней недели за последние три месяца.
    
    Args:
        transactions: Датафрейм с транзакциями
        date: Опциональная дата (формат: 'YYYY-MM-DD'). 
              Если не передана, используется текущая дата
    
    Returns:
        Датафрейм со средними тратами по дням недели за последние три месяца
    """
    pass
```

**Пример использования:**
```python
import pandas as pd
from src.reports import spending_by_weekday

df = pd.read_excel("data/operations.xlsx")
result = spending_by_weekday(df, date="2021-12-31")
# Возвращает средние траты по дням недели за последние 3 месяца от 2021-12-31
```

#### Траты в рабочий/выходной день
- **Модуль**: `src/reports.py`
- **Функция**: `spending_by_workday(transactions: pd.DataFrame, date: Optional[str] = None) -> pd.DataFrame`
- **Теги**: `#json`, `#pandas`, `#logging`, `#pytest`, `#datetime`
- **Описание**: Выводит средние траты в рабочий и в выходной день за последние три месяца (от переданной даты)
- **Входные параметры:**
  - `transactions` - датафрейм с транзакциями
  - `date` - опциональная дата (если не передана, берется текущая дата)
- **Выходные данные:** Датафрейм со средними тратами в рабочие и выходные дни за последние три месяца
- **Используемые библиотеки**: `json`, `pandas`, `logging`, `datetime`

**Интерфейс функции:**
```python
def spending_by_workday(transactions: pd.DataFrame, 
                        date: Optional[str] = None) -> pd.DataFrame:
    """
    Возвращает средние траты в рабочий и в выходной день за последние три месяца.
    
    Args:
        transactions: Датафрейм с транзакциями
        date: Опциональная дата (формат: 'YYYY-MM-DD'). 
              Если не передана, используется текущая дата
    
    Returns:
        Датафрейм со средними тратами в рабочие и выходные дни за последние три месяца
    """
    pass
```

**Пример использования:**
```python
import pandas as pd
from src.reports import spending_by_workday

df = pd.read_excel("data/operations.xlsx")
result = spending_by_workday(df, date="2021-12-31")
# Возвращает средние траты в рабочие и выходные дни за последние 3 месяца от 2021-12-31
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Запуск всех тестов
poetry run pytest

# Запуск с покрытием кода
poetry run pytest --cov=src --cov-report=html

# Запуск конкретного файла тестов
poetry run pytest tests/test_views.py
```

### Требования к тестам

- Покрытие кода: не менее 80%
- Использование `Mock` и `patch` для изоляции зависимостей
- Использование фикстур для генерации тестовых данных
- Параметризация тестов

## 🔍 Проверка качества кода

### Форматирование кода

```bash
# Форматирование с помощью Black
poetry run black .

# Сортировка импортов с помощью isort
poetry run isort .

# Проверка стиля с помощью Flake8
poetry run flake8 .

# Проверка типов с помощью mypy
poetry run mypy src/
```

### Конфигурация инструментов

- **Black**: line-length = 119
- **isort**: line_length = 119, profile = "black"
- **Flake8**: max-line-length = 119
- **mypy**: disallow_untyped_defs = true, warn_return_any = true

## 📝 Правила разработки

Проект следует правилам разработки, описанным в:
- `Правила-разработки.md` - краткие правила и принципы
- `инструкция_разработки.md` - подробные пошаговые инструкции

### Основные принципы

1. **База знаний - наивысший приоритет** - используем подходы из базы знаний
2. **Zen of Python** - следуем принципам PEP 20
3. **Типизация** - все функции должны иметь type hints
4. **Документация** - все функции должны иметь docstrings (Google style)
5. **Логирование** - используем логирование для важных операций
6. **Обработка ошибок** - явная обработка исключений

## 🔐 Безопасность

### ⚠️ Важные правила безопасности

1. **Никогда не коммитьте конфиденциальные данные:**
   - API ключи и токены должны храниться только в файле `.env`
   - Файл `.env` добавлен в `.gitignore` и не попадет в репозиторий
   - Используйте `.env.example` как шаблон без реальных значений

2. **Защита данных транзакций:**
   - Файлы с транзакциями (`data/operations.xlsx`) не должны содержать полные номера карт
   - В данных используются только последние 4 цифры номера карты
   - Папка `data/` уже добавлена в `.gitignore` и не попадет в репозиторий
   - Не коммитьте файлы с реальными транзакциями

3. **Работа с API:**
   - Все API ключи должны храниться в переменных окружения
   - Не логируйте API ключи и токены
   - Используйте HTTPS для всех API-запросов

4. **Проверка перед коммитом:**
   - Убедитесь, что в коммит не попали файлы `.env`
   - Проверьте, что нет реальных API ключей в коде
   - Убедитесь, что файлы из папки `data/` не добавлены в коммит
   - Используйте `git status` перед коммитом
   - Проверьте содержимое коммита: `git diff --cached`

5. **Работа с Google Sheets:**
   - Ссылка на Google Sheets в README является публичной для доступа к данным
   - Убедитесь, что документ имеет правильные настройки доступа
   - При экспорте данных проверьте, что файл не содержит конфиденциальной информации

6. **Файлы проекта:**
   - `user_settings.json` - может быть публичным (содержит только валюты и акции, не конфиденциально)
   - `.env` - **НИКОГДА** не коммитить (содержит API ключи)
   - `data/operations.xlsx` - **НИКОГДА** не коммитить (содержит данные транзакций)
   - `.env.example` - безопасен для коммита (содержит только шаблоны)

### Переменные окружения

Создайте файл `.env` на основе `.env.example` и заполните необходимые переменные:

```env
# API настройки
API_KEY=your_api_key_here
API_URL=https://api.example.com

# Другие настройки
LOG_LEVEL=INFO
DATA_DIR=./data
```

**⚠️ ВАЖНО:** Замените `your_api_key_here` на реальный API ключ только в файле `.env`, который не коммитится в Git!

## 📦 Зависимости

### Основные зависимости

- `requests` - для работы с HTTP API
- `pandas` - для работы с данными и Excel-файлами

### Зависимости для разработки

- `pytest` - тестирование
- `black` - форматирование кода
- `flake8` - проверка стиля
- `isort` - сортировка импортов
- `mypy` - проверка типов

Полный список зависимостей см. в `pyproject.toml`.

## 🛠️ Разработка

### Создание новой функции

1. Следуйте инструкции разработки (`инструкция_разработки.md`)
2. Используйте подходы из базы знаний
3. Добавьте type hints и docstrings
4. Напишите тесты
5. Проверьте код линтерами

### Коммиты

Используйте формат коммитов:
```
<Тип>: <Краткое описание>

<Подробное описание (опционально)>
```

Типы: `feat`, `fix`, `refactor`, `docs`, `test`, `style`, `chore`

## 📄 Лицензия

Проект распространяется под лицензией [MIT License](LICENSE).

## 👥 Авторы

[Укажите авторов проекта]

## 📞 Контакты

[Укажите контактную информацию]

