# Использование

Это руководство описывает, как использовать различные модули и функции проекта Transaction Analyzer.

## 🚀 Быстрый старт

### Запуск веб-сервера

Самый простой способ начать работу - запустить веб-сервер:

```bash
poetry run python web_server.py
```

Сервер запустится на `http://localhost:5001`. Откройте этот адрес в браузере.

### Запуск главного модуля

Для демонстрации всех функций приложения:

```bash
poetry run python main.py
```

Этот модуль демонстрирует работу:
- Веб-страниц (views)
- Сервисов (services)
- Отчетов (reports)

## 📊 Веб-страницы

### Главная страница

Функция `home_page()` генерирует JSON-данные для главной страницы.

**Использование:**

```python
from src.utils import load_transactions_from_excel
from src.views import home_page
import pandas as pd

# Загрузка данных
df = load_transactions_from_excel("data/operations.xlsx")

# Генерация данных для главной страницы
data = home_page("2024-03-15 14:30:00", df)

print(data["greeting"])  # "Добрый день"
print(data["cards"])     # Список карт с расходами и кешбэком
```

**Параметры:**
- `date_time` (str): Дата и время в формате `YYYY-MM-DD HH:MM:SS`
- `transactions` (pd.DataFrame): DataFrame с транзакциями

**Возвращает:**
- `greeting` - Приветствие по времени суток
- `cards` - Список карт с расходами и кешбэком
- `top_transactions` - Топ-5 транзакций
- `currency_rates` - Курсы валют
- `stock_prices` - Цены акций

**Диапазон данных:** С начала месяца по указанную дату

### Страница событий

Функция `events_page()` генерирует JSON-данные для страницы событий.

**Использование:**

```python
from src.utils import load_transactions_from_excel
from src.views import events_page

# Загрузка данных
df = load_transactions_from_excel("data/operations.xlsx")

# Генерация данных для страницы событий
data = events_page("2024-03-15", "M", df)

print(data["expenses"]["total_amount"])  # Общая сумма расходов
print(data["income"]["total_amount"])     # Общая сумма поступлений
```

**Параметры:**
- `date` (str): Дата в формате `YYYY-MM-DD`
- `period` (str): Период данных
  - `"W"` - Неделя (последние 7 дней)
  - `"M"` - Месяц (с начала месяца)
  - `"Y"` - Год (с начала года)
  - `"ALL"` - Все данные
  - `"CUSTOM"` - Кастомный диапазон (требует `start_date` и `end_date`)
- `transactions` (pd.DataFrame): DataFrame с транзакциями
- `start_date` (str, optional): Начальная дата для периода CUSTOM
- `end_date` (str, optional): Конечная дата для периода CUSTOM
- `card_filter` (str, optional): Последние 4 цифры карты для фильтрации

**Возвращает:**
- `expenses` - Расходы (общая сумма, категории, переводы и наличные)
- `income` - Поступления (общая сумма, категории)
- `currency_rates` - Курсы валют
- `stock_prices` - Цены акций

## 🔍 Сервисы

### Выгодные категории кешбэка

Находит категории с наибольшим кешбэком за указанный месяц.

```python
from src.services import profitable_cashback_categories
from src.utils import load_transactions_from_excel

# Загрузка данных
df = load_transactions_from_excel("data/operations.xlsx")
transactions = df.to_dict(orient="records")

# Анализ категорий кешбэка
categories = profitable_cashback_categories(transactions, year=2024, month=3)

for category, cashback in categories.items():
    print(f"{category}: {cashback:.2f} руб.")
```

### Инвесткопилка

Рассчитывает сумму округлений для инвесткопилки.

```python
from src.services import investment_bank

# Расчет за март 2024
amount = investment_bank("2024-03", transactions, limit=50)
print(f"Сумма округлений: {amount:.2f} руб.")
```

**Параметры:**
- `month` (str): Месяц в формате `YYYY-MM`
- `transactions` (List[Dict]): Список транзакций
- `limit` (int): Лимит округления (например, 50 для округления до 50)

### Простой поиск

Поиск транзакций по запросу в описании или категории.

```python
from src.services import simple_search

result = simple_search("Лента", transactions)
print(f"Найдено транзакций: {len(result['transactions'])}")
```

### Поиск по телефонным номерам

Находит транзакции, содержащие телефонные номера в описании.

```python
from src.services import search_by_phone

result = search_by_phone(transactions)
print(f"Найдено транзакций с номерами: {len(result['transactions'])}")
```

### Поиск переводов физическим лицам

Находит переводы физическим лицам (по имени в описании).

```python
from src.services import search_person_transfers

result = search_person_transfers(transactions)
print(f"Найдено переводов: {len(result['transactions'])}")
```

## 📈 Отчеты

Все отчеты автоматически сохраняются в папку `reports/` благодаря декоратору `@save_report()`.

### Траты по категории

Анализирует траты по указанной категории за последние 3 месяца.

```python
from src.reports import spending_by_category

# Анализ трат по категории "Супермаркеты"
report = spending_by_category(df, category="Супермаркеты", date="2024-03-15")
print(report)
```

**Параметры:**
- `transactions` (pd.DataFrame): DataFrame с транзакциями
- `category` (str): Категория для анализа
- `date` (str, optional): Дата отсчета (по умолчанию текущая дата)

### Траты по дням недели

Анализирует средние траты по дням недели за последние 3 месяца.

```python
from src.reports import spending_by_weekday

report = spending_by_weekday(df, date="2024-03-15")
print(report)
```

### Траты в рабочий/выходной день

Анализирует средние траты в рабочие и выходные дни.

```python
from src.reports import spending_by_workday

report = spending_by_workday(df, date="2024-03-15")
print(report)
```

## 🌐 Веб-интерфейс

### Запуск веб-сервера

```bash
poetry run python web_server.py
```

Сервер запустится на `http://localhost:5001`.

### Использование веб-интерфейса

1. **Главная страница** (`/`)
   - Отображает обзор транзакций
   - Показывает данные по картам
   - Топ-5 транзакций
   - Курсы валют и цены акций

2. **Страница событий** (`/`)
   - Фильтрация по периодам (Неделя, Месяц, Год, Все)
   - Выбор диапазона дат
   - Фильтрация по картам
   - Расходы и поступления по категориям

### API Endpoints

#### GET /api/home

Возвращает данные для главной страницы.

**Ответ:**
```json
{
  "greeting": "Добрый день",
  "cards": [...],
  "top_transactions": [...],
  "currency_rates": [...],
  "stock_prices": [...]
}
```

#### GET /api/events?period=M

Возвращает данные для страницы событий.

**Параметры:**
- `period` (str): Период (W, M, Y, ALL, CUSTOM)
- `start_date` (str, optional): Начальная дата (для CUSTOM)
- `end_date` (str, optional): Конечная дата (для CUSTOM)
- `card` (str, optional): Последние 4 цифры карты

**Ответ:**
```json
{
  "expenses": {
    "total_amount": 12345,
    "main": [...],
    "transfers_and_cash": [...]
  },
  "income": {
    "total_amount": 50000,
    "main": [...]
  },
  "currency_rates": [...],
  "stock_prices": [...]
}
```

## 💡 Примеры использования

### Полный пример анализа транзакций

```python
from src.utils import load_transactions_from_excel
from src.views import home_page, events_page
from src.services import profitable_cashback_categories, investment_bank
from src.reports import spending_by_category
import pandas as pd

# 1. Загрузка данных
df = load_transactions_from_excel("data/operations.xlsx")
transactions = df.to_dict(orient="records")

# 2. Генерация данных для веб-страниц
home_data = home_page("2024-03-15 14:30:00", df)
events_data = events_page("2024-03-15", "M", df)

# 3. Анализ категорий кешбэка
cashback_categories = profitable_cashback_categories(transactions, 2024, 3)

# 4. Расчет инвесткопилки
investment = investment_bank("2024-03", transactions, limit=50)

# 5. Генерация отчета
report = spending_by_category(df, "Супермаркеты", "2024-03-15")

print("Анализ завершен!")
```

### Работа с фильтрами

```python
# Фильтрация по кастомному диапазону дат
events_data = events_page(
    "",
    "CUSTOM",
    df,
    start_date="2024-01-01",
    end_date="2024-03-31"
)

# Фильтрация по карте
events_data = events_page(
    "2024-03-15",
    "M",
    df,
    card_filter="7197"
)

# Комбинация фильтров
events_data = events_page(
    "",
    "CUSTOM",
    df,
    start_date="2024-01-01",
    end_date="2024-03-31",
    card_filter="7197"
)
```

## 📚 Дополнительная информация

- [Веб-интерфейс](Web-Interface) - Подробное руководство по веб-интерфейсу
- [API Reference](API-Reference) - Справочник по API
- [Решение проблем](Troubleshooting) - Решение проблем при использовании

---

**Следующий шаг:** [Веб-интерфейс](Web-Interface)

