# Установка

Это руководство поможет вам установить и настроить Transaction Analyzer на вашей системе.

## 📋 Требования

Перед установкой убедитесь, что у вас установлены:

- **Python 3.9 или выше** - [Скачать Python](https://www.python.org/downloads/)
- **Poetry** - Менеджер зависимостей для Python - [Установка Poetry](https://python-poetry.org/docs/#installation)

### Проверка версий

```bash
python3 --version  # Должно быть 3.9 или выше
poetry --version   # Должна быть последняя версия
```

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/transaction-analyzer.git
cd transaction-analyzer
```

### 2. Установка зависимостей

Установите все зависимости проекта через Poetry:

```bash
poetry install
```

Эта команда:
- Создаст виртуальное окружение
- Установит все зависимости из `pyproject.toml`
- Установит зависимости для разработки (pytest, black, flake8, mypy и т.д.)

### 3. Активация виртуального окружения

```bash
poetry shell
```

После выполнения этой команды вы окажетесь в активированном виртуальном окружении.

**Альтернатива:** Вы можете запускать команды без активации окружения, используя `poetry run`:

```bash
poetry run python main.py
```

## ⚙️ Настройка окружения

### 1. Создание файла `.env`

Скопируйте шаблон конфигурации:

```bash
cp .env.example .env
```

### 2. Настройка переменных окружения

Откройте файл `.env` и заполните необходимые переменные:

```env
# API настройки
API_KEY=your_api_key_here
API_URL=https://api.exchangerate-api.com/v4

# Настройки логирования
LOG_LEVEL=INFO

# Настройки данных
DATA_DIR=./data
EXCEL_FILE=data/operations.xlsx

# Другие настройки
DEFAULT_CURRENCY=RUB
```

**Примечание:** Если у вас нет API ключа, приложение автоматически использует бесплатный API `exchangerate-api.com`, который не требует ключа.

### 3. Настройка данных

#### Подготовка файла транзакций

1. Экспортируйте данные из вашего Google Sheets документа:
   - Откройте таблицу с транзакциями
   - Файл → Скачать → Microsoft Excel (`.xlsx`)
   - Сохраните файл как `operations.xlsx` в папку `data/` проекта
   
   **Примечание:** Убедитесь, что ваш Google Sheets документ имеет правильные настройки доступа и не содержит конфиденциальной информации при экспорте.

2. Проверьте наличие файла:

```bash
ls data/operations.xlsx
```

#### Настройка пользовательских параметров

Создайте или отредактируйте файл `user_settings.json` в корне проекта:

```json
{
  "user_currencies": ["USD", "EUR"],
  "user_stocks": ["AAPL", "AMZN", "GOOGL", "MSFT", "TSLA"]
}
```

Этот файл определяет:
- `user_currencies` - список валют для отображения курсов
- `user_stocks` - список акций для отображения цен

## ✅ Проверка установки

### Запуск тестов

Убедитесь, что все тесты проходят:

```bash
poetry run pytest
```

### Запуск приложения

Запустите главный модуль:

```bash
poetry run python main.py
```

Или запустите веб-сервер:

```bash
poetry run python web_server.py
```

Затем откройте браузер и перейдите по адресу: `http://localhost:5001`

## 🐛 Решение проблем

### Проблема: Poetry не найден

**Решение:** Установите Poetry согласно [официальной документации](https://python-poetry.org/docs/#installation).

### Проблема: Ошибка при установке зависимостей

**Решение:** 
1. Убедитесь, что у вас установлена последняя версия Poetry: `poetry self update`
2. Попробуйте очистить кеш: `poetry cache clear pypi --all`
3. Переустановите зависимости: `poetry install --no-cache`

### Проблема: Файл `data/operations.xlsx` не найден

**Решение:** 
1. Убедитесь, что файл существует: `ls data/operations.xlsx`
2. Проверьте путь в `.env`: `EXCEL_FILE=data/operations.xlsx`
3. Создайте папку `data/`, если её нет: `mkdir -p data`

### Проблема: Порт 5001 уже занят

**Решение:** 
1. Найдите процесс, использующий порт: `lsof -ti:5001`
2. Остановите процесс: `kill -9 <PID>`
3. Или измените порт в `web_server.py`: `DEFAULT_PORT = 5002`

## 📚 Дополнительная информация

- [Конфигурация](Configuration) - Подробная информация о настройках
- [Использование](Usage) - Руководство по использованию приложения
- [Решение проблем](Troubleshooting) - Дополнительные решения проблем

---

**Следующий шаг:** [Конфигурация](Configuration)

