# Внесение вклада

Спасибо за интерес к проекту Transaction Analyzer! Мы приветствуем вклад от сообщества.

## 🤝 Как внести вклад

### Сообщение об ошибках

Если вы нашли ошибку:

1. Проверьте [Issues](https://github.com/your-username/transaction-analyzer/issues), возможно, проблема уже известна
2. Создайте новый Issue с:
   - Четким описанием проблемы
   - Шагами для воспроизведения
   - Ожидаемым и фактическим поведением
   - Версией Python, Poetry и ОС
   - Соответствующими логами

### Предложение улучшений

Если у вас есть идея для улучшения:

1. Создайте Issue с меткой `enhancement`
2. Опишите предлагаемое улучшение
3. Объясните, почему это улучшение полезно

### Отправка Pull Request

1. **Fork репозитория**
2. **Создайте ветку для ваших изменений:**
   ```bash
   git checkout -b feature/your-feature-name
   ```
3. **Внесите изменения:**
   - Следуйте стилю кода проекта
   - Добавьте тесты для новых функций
   - Обновите документацию
4. **Запустите тесты:**
   ```bash
   poetry run pytest
   poetry run flake8 src tests
   poetry run mypy src
   ```
5. **Закоммитьте изменения:**
   ```bash
   git commit -m "feat: описание вашего изменения"
   ```
6. **Отправьте Pull Request:**
   - Опишите, что было изменено
   - Укажите связанные Issues
   - Приложите скриншоты, если применимо

## 📋 Стандарты кода

### Стиль кода

Проект использует следующие инструменты для обеспечения качества кода:

- **Black** - Форматирование кода (длина строки: 119 символов)
- **Flake8** - Линтинг (длина строки: 119 символов)
- **isort** - Сортировка импортов
- **mypy** - Проверка типов

**Проверка перед коммитом:**
```bash
poetry run black src tests
poetry run isort src tests
poetry run flake8 src tests
poetry run mypy src
```

### Структура модуля

Каждый модуль должен следовать этой структуре:

```python
"""Краткое описание модуля.

Подробное описание модуля и его назначения.
"""

# Стандартная библиотека
import logging
from pathlib import Path
from typing import Any, Dict, List

# Сторонние библиотеки
import pandas as pd  # type: ignore[import-untyped]

# Локальные импорты
from src.utils import some_function

# Константы модуля
ENCODING = "utf-8"
DEFAULT_VALUE = 0

# Настройка логгера
def _setup_logger() -> logging.Logger:
    """Настраивает и возвращает логгер для модуля."""
    # ...

logger = _setup_logger()

# Функции модуля
def my_function(param: str) -> Dict[str, Any]:
    """Описание функции.

    Args:
        param: Описание параметра

    Returns:
        Описание возвращаемого значения

    Example:
        >>> result = my_function("test")
        >>> print(result)
        {"key": "value"}
    """
    logger.info(f"Выполнение функции с параметром: {param}")
    # ...
```

### Документация

- Все функции должны иметь docstrings в формате Google
- Используйте type hints для всех параметров и возвращаемых значений
- Добавляйте примеры использования в docstrings

### Логирование

- Используйте соответствующий уровень логирования:
  - `DEBUG` - Детальная информация для отладки
  - `INFO` - Общая информация о работе
  - `WARNING` - Предупреждения о потенциальных проблемах
  - `ERROR` - Ошибки, которые не останавливают выполнение
  - `CRITICAL` - Критические ошибки
- Формат сообщений: `logger.error(f"Ошибка: {type(e).__name__} - {e}")`

### Обработка ошибок

- Всегда обрабатывайте исключения явно
- Логируйте ошибки с типом исключения
- Возвращайте безопасные значения по умолчанию

```python
try:
    result = risky_operation()
except SpecificException as e:
    logger.error(f"Ошибка при выполнении операции: {type(e).__name__} - {e}")
    return default_value
```

## 🧪 Тестирование

### Написание тестов

- Тесты должны быть в папке `tests/`
- Имя файла: `test_<module_name>.py`
- Используйте фикстуры для тестовых данных
- Используйте `Mock` и `patch` для изоляции зависимостей
- Параметризуйте тесты, где это уместно

**Пример:**
```python
import pytest
from unittest.mock import patch, MagicMock
from src.views import home_page

@pytest.fixture
def sample_transactions():
    """Фикстура с тестовыми данными."""
    return pd.DataFrame({
        "Дата операции": [...],
        "Сумма платежа": [...],
        # ...
    })

@patch("src.views.get_currency_rates")
def test_home_page_success(mock_get_currency, sample_transactions):
    """Тест успешной генерации данных."""
    mock_get_currency.return_value = []
    result = home_page("2024-03-15 14:30:00", sample_transactions)
    assert "greeting" in result
```

### Запуск тестов

```bash
# Все тесты
poetry run pytest

# С покрытием
poetry run pytest --cov=src --cov-report=term-missing

# Конкретный тест
poetry run pytest tests/test_views.py::test_home_page_success

# С подробным выводом
poetry run pytest -v
```

### Требования к покрытию

- Минимальное покрытие: 80%
- Новый код должен иметь покрытие не менее 90%

## 📝 Коммиты

### Формат коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Типы:**
- `feat` - Новая функция
- `fix` - Исправление ошибки
- `docs` - Изменения в документации
- `style` - Форматирование кода
- `refactor` - Рефакторинг
- `test` - Добавление/изменение тестов
- `chore` - Обновление зависимостей, конфигурации

**Примеры:**
```bash
git commit -m "feat(views): добавлена фильтрация по карте"
git commit -m "fix(utils): исправлена обработка ошибок API"
git commit -m "docs: обновлена документация по установке"
```

## 🔄 Процесс разработки

### Workflow

1. **Создайте Issue** для обсуждения изменений
2. **Fork репозитория**
3. **Создайте ветку** от `develop` (если есть) или `main`
4. **Внесите изменения** и добавьте тесты
5. **Запустите проверки:**
   ```bash
   poetry run pytest
   poetry run flake8 src tests
   poetry run mypy src
   poetry run black --check src tests
   poetry run isort --check src tests
   ```
6. **Отправьте Pull Request** в ветку `develop` или `main`

### Code Review

- Все Pull Request проходят code review
- Отвечайте на комментарии ревьюеров
- Вносите необходимые изменения
- Не закрывайте Pull Request до завершения review

## 📚 Дополнительные ресурсы

### Полезные ссылки

- [Стиль кода проекта](bank-code/СТИЛЬ-КОДА.md)
- [Шаблоны кода](bank-code/ШАБЛОНЫ-КОДА.md)
- [Справочник](bank-code/СПРАВОЧНИК.md)
- [Правила разработки](База-знаний/Правила-разработки.md)

### Инструменты разработки

- **Poetry** - [Документация](https://python-poetry.org/docs/)
- **Pytest** - [Документация](https://docs.pytest.org/)
- **Black** - [Документация](https://black.readthedocs.io/)
- **mypy** - [Документация](https://mypy.readthedocs.io/)

## ❓ Вопросы

Если у вас есть вопросы:

1. Проверьте [документацию](Home)
2. Поищите в [Issues](https://github.com/your-username/transaction-analyzer/issues)
3. Создайте новый Issue с вопросом

## 🙏 Благодарности

Спасибо всем, кто вносит вклад в проект! Ваша помощь делает проект лучше.

---

**Следующий шаг:** [Home](Home) - Вернуться к главной странице документации

