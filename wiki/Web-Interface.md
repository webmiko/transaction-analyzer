# Веб-интерфейс

Transaction Analyzer включает полнофункциональный веб-интерфейс для визуализации и анализа транзакций.

## 🚀 Запуск веб-сервера

```bash
poetry run python web_server.py
```

Сервер запустится на `http://localhost:5001` (по умолчанию).

**Примечание:** Если порт 5001 занят, измените `DEFAULT_PORT` в `web_server.py`.

## 📱 Интерфейс

### Главная страница

Главная страница (`/`) отображает:

#### Приветствие
- Динамическое приветствие в зависимости от времени суток:
  - **5:00-11:59** - "Доброе утро"
  - **12:00-16:59** - "Добрый день"
  - **17:00-21:59** - "Добрый вечер"
  - **22:00-4:59** - "Доброй ночи"

#### Данные по картам
- Последние 4 цифры каждой карты
- Общая сумма расходов
- Кешбэк (1% от суммы расходов)

#### Топ-5 транзакций
- Дата транзакции
- Сумма платежа
- Категория
- Описание

#### Курсы валют
- Список валют из `user_settings.json`
- Текущие курсы относительно рубля

#### Цены акций
- Список акций из `user_settings.json`
- Текущие цены акций

**Диапазон данных:** С начала месяца по максимальную дату в данных

### Страница событий

Страница событий предоставляет расширенные возможности фильтрации:

#### Фильтры по периоду
- **Неделя** - Последние 7 дней
- **Месяц** - С начала месяца
- **Год** - С начала года
- **Все** - Все доступные данные

#### Выбор диапазона дат
- **Дата начала** - Начальная дата диапазона
- **Дата конца** - Конечная дата диапазона
- **Применить** - Применить выбранный диапазон

#### Фильтр по карте
- Выпадающий список всех доступных карт
- Опция "Все карты" для отключения фильтра
- Автоматическое применение при выборе

#### Расходы
- Общая сумма расходов
- Основные категории (топ-7)
- Переводы и наличные (отдельно)

#### Поступления
- Общая сумма поступлений
- Категории поступлений

#### Курсы валют и акции
- Те же данные, что и на главной странице

## 🔧 Использование фильтров

### Фильтрация по периоду

1. Нажмите на кнопку периода (Неделя, Месяц, Год, Все)
2. Данные автоматически обновятся

### Фильтрация по диапазону дат

1. Выберите дату начала в поле "Дата начала"
2. Выберите дату конца в поле "Дата конца"
3. Нажмите кнопку "Применить"
4. Данные обновятся для выбранного диапазона

**Примечание:** Дата начала не может быть позже даты конца.

### Фильтрация по карте

1. Выберите карту из выпадающего списка
2. Данные автоматически обновятся
3. Выберите "Все карты" для отключения фильтра

### Комбинирование фильтров

Все фильтры работают совместно:
- Можно выбрать период И диапазон дат И карту
- При выборе кастомного диапазона дат кнопки периода деактивируются
- Фильтр по карте работает с любыми другими фильтрами

## 📡 API Endpoints

### GET /api/home

Возвращает данные для главной страницы.

**Запрос:**
```http
GET /api/home
```

**Ответ:**
```json
{
  "greeting": "Добрый день",
  "cards": [
    {
      "last_digits": "7197",
      "total_spent": 24576.63,
      "cashback": 245.77
    }
  ],
  "top_transactions": [
    {
      "date": "30.12.2021",
      "amount": 174000.00,
      "category": "Пополнения",
      "description": "Пополнение через Газпромбанк"
    }
  ],
  "currency_rates": [
    {
      "currency": "USD",
      "rate": 79.37
    }
  ],
  "stock_prices": [
    {
      "stock": "AAPL",
      "price": 150.12
    }
  ]
}
```

### GET /api/events

Возвращает данные для страницы событий.

**Запрос:**
```http
GET /api/events?period=M&card=7197
```

**Параметры:**
- `period` (str): Период (W, M, Y, ALL, CUSTOM)
- `start_date` (str, optional): Начальная дата для CUSTOM (формат: YYYY-MM-DD)
- `end_date` (str, optional): Конечная дата для CUSTOM (формат: YYYY-MM-DD)
- `card` (str, optional): Последние 4 цифры карты

**Ответ:**
```json
{
  "expenses": {
    "total_amount": 12345,
    "main": [
      {
        "category": "Супермаркеты",
        "amount": 5000
      }
    ],
    "transfers_and_cash": [
      {
        "category": "Переводы",
        "amount": 2000
      }
    ]
  },
  "income": {
    "total_amount": 50000,
    "main": [
      {
        "category": "Пополнения",
        "amount": 50000
      }
    ]
  },
  "currency_rates": [...],
  "stock_prices": [...]
}
```

## 🎨 Особенности интерфейса

### Адаптивный дизайн
- Интерфейс адаптируется под размер экрана
- Оптимизирован для мобильных устройств

### Темная/светлая тема
- Автоматическое определение темы браузера
- Четкая читаемость текста на всех фонах

### Автоматическое обновление
- Данные загружаются автоматически при открытии страницы
- Обновление при переключении между вкладками

### Обработка ошибок
- Информативные сообщения об ошибках
- Graceful degradation при недоступности API

## 🔒 Безопасность

### CORS
- Сервер настроен для работы с CORS
- Разрешены запросы с любых источников (для разработки)

**Примечание:** Для продакшена настройте CORS более строго.

### Защита данных
- API ключи не передаются в ответах
- Ошибки API санитизируются перед логированием

## 🐛 Решение проблем

### Страница не загружается

**Проблема:** Страница показывает "Ошибка: Load failed"

**Решение:**
1. Убедитесь, что сервер запущен: `poetry run python web_server.py`
2. Проверьте, что порт 5001 не занят: `lsof -ti:5001`
3. Проверьте консоль браузера на наличие ошибок

### Данные не отображаются

**Проблема:** Транзакции не отображаются на странице

**Решение:**
1. Убедитесь, что файл `data/operations.xlsx` существует
2. Проверьте логи сервера: `tail -f logs/views.log`
3. Убедитесь, что данные в файле соответствуют ожидаемому формату

### Курсы валют не отображаются

**Проблема:** Таблица валют пуста

**Решение:**
1. Проверьте `user_settings.json` - должен содержать `user_currencies`
2. Проверьте логи: `tail -f logs/utils.log`
3. Приложение автоматически использует бесплатный API, если основной не работает

### Фильтры не работают

**Проблема:** Фильтры не применяются

**Решение:**
1. Проверьте консоль браузера на наличие ошибок JavaScript
2. Убедитесь, что выбранные даты корректны
3. Проверьте, что карта существует в данных

## 📚 Дополнительная информация

- [Использование](Usage) - Общее руководство по использованию
- [API Reference](API-Reference) - Подробный справочник по API
- [Решение проблем](Troubleshooting) - Дополнительные решения проблем

---

**Следующий шаг:** [API Reference](API-Reference)

