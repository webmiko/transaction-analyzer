# Решение проблем

Этот раздел содержит решения распространенных проблем при работе с Transaction Analyzer.

## 🔧 Проблемы установки

### Poetry не найден

**Проблема:** Команда `poetry` не распознается

**Решение:**
1. Установите Poetry согласно [официальной документации](https://python-poetry.org/docs/#installation)
2. Для macOS/Linux:
   ```bash
   curl -sSL https://install.python-poetry.org | python3 -
   ```
3. Добавьте Poetry в PATH:
   ```bash
   export PATH="$HOME/.local/bin:$PATH"
   ```

### Ошибка при установке зависимостей

**Проблема:** `poetry install` завершается с ошибкой

**Решение:**
1. Обновите Poetry:
   ```bash
   poetry self update
   ```
2. Очистите кеш:
   ```bash
   poetry cache clear pypi --all
   ```
3. Переустановите зависимости:
   ```bash
   poetry install --no-cache
   ```

### Проблемы с виртуальным окружением

**Проблема:** Python не найден в виртуальном окружении

**Решение:**
1. Пересоздайте виртуальное окружение:
   ```bash
   poetry env remove python
   poetry install
   ```
2. Проверьте версию Python:
   ```bash
   poetry run python --version
   ```

## 📁 Проблемы с данными

### Файл operations.xlsx не найден

**Проблема:** `FileNotFoundError: data/operations.xlsx`

**Решение:**
1. Убедитесь, что файл существует:
   ```bash
   ls data/operations.xlsx
   ```
2. Проверьте путь в `.env`:
   ```env
   EXCEL_FILE=data/operations.xlsx
   ```
3. Создайте папку `data/`, если её нет:
   ```bash
   mkdir -p data
   ```
4. Экспортируйте данные из Google Sheets и сохраните в `data/operations.xlsx`

### Некорректный формат данных

**Проблема:** Ошибки при чтении Excel файла

**Решение:**
1. Убедитесь, что файл имеет формат `.xlsx` (не `.xls`)
2. Проверьте, что все обязательные колонки присутствуют:
   - Дата операции
   - Дата платежа
   - Номер карты
   - Статус
   - Сумма операции
   - Валюта операции
   - Сумма платежа
   - Валюта платежа
   - Кэшбэк
   - Категория
   - MCC
   - Описание
   - Бонусы (включая кэшбэк)
   - Округление на инвесткопилку
   - Сумма операции с округлением
3. Проверьте формат дат:
   - Дата операции: `DD.MM.YYYY HH:MM:SS`
   - Дата платежа: `DD.MM.YYYY`

### Пустой DataFrame после загрузки

**Проблема:** `df.empty` возвращает `True`

**Решение:**
1. Проверьте, что файл не пустой
2. Проверьте, что данные находятся на первом листе Excel
3. Проверьте логи: `tail -f logs/utils.log`

## 🌐 Проблемы с веб-сервером

### Порт 5001 уже занят

**Проблема:** `Port 5000 is in use by another program`

**Решение:**
1. Найдите процесс, использующий порт:
   ```bash
   lsof -ti:5001
   ```
2. Остановите процесс:
   ```bash
   kill -9 <PID>
   ```
3. Или измените порт в `web_server.py`:
   ```python
   DEFAULT_PORT = 5002  # или другой свободный порт
   ```

### Страница не загружается

**Проблема:** Страница показывает "Ошибка: Load failed"

**Решение:**
1. Убедитесь, что сервер запущен:
   ```bash
   poetry run python web_server.py
   ```
2. Проверьте, что сервер отвечает:
   ```bash
   curl http://localhost:5001/api/home
   ```
3. Проверьте консоль браузера (F12) на наличие ошибок
4. Проверьте логи сервера: `tail -f logs/views.log`

### Данные не отображаются на странице

**Проблема:** Транзакции не отображаются

**Решение:**
1. Проверьте, что файл `data/operations.xlsx` существует и содержит данные
2. Проверьте логи: `tail -f logs/views.log`
3. Убедитесь, что данные в файле соответствуют ожидаемому формату
4. Проверьте, что даты в данных не в будущем (приложение использует максимальную дату из данных)

## 💱 Проблемы с API

### Курсы валют не отображаются

**Проблема:** Таблица валют пуста

**Решение:**
1. Проверьте `user_settings.json`:
   ```json
   {
     "user_currencies": ["USD", "EUR"]
   }
   ```
2. Проверьте логи: `tail -f logs/utils.log`
3. Приложение автоматически использует бесплатный API, если основной не работает
4. Если проблема сохраняется, проверьте интернет-соединение

### Ошибка 401 Unauthorized

**Проблема:** `HTTPError - 401 Client Error: Unauthorized`

**Решение:**
1. Приложение автоматически переключается на бесплатный API при ошибке 401
2. Если проблема сохраняется, проверьте логи: `tail -f logs/utils.log`
3. Убедитесь, что интернет-соединение активно

### Цены акций не отображаются

**Проблема:** Таблица акций пуста

**Решение:**
1. Проверьте `user_settings.json`:
   ```json
   {
     "user_stocks": ["AAPL", "AMZN"]
   }
   ```
2. Для получения цен акций требуется API ключ Alpha Vantage
3. Если ключ не указан, цены акций не будут отображаться (это нормально)
4. Проверьте логи: `tail -f logs/utils.log`

## 🔍 Проблемы с фильтрами

### Фильтры не работают

**Проблема:** Фильтры не применяются на странице событий

**Решение:**
1. Проверьте консоль браузера (F12) на наличие ошибок JavaScript
2. Убедитесь, что выбранные даты корректны (дата начала <= дата конца)
3. Проверьте, что карта существует в данных
4. Обновите страницу (Ctrl+F5 или Cmd+Shift+R)

### Неправильный диапазон дат

**Проблема:** Фильтр по датам показывает неправильные данные

**Решение:**
1. Убедитесь, что формат дат правильный: `YYYY-MM-DD`
2. Проверьте, что дата начала не позже даты конца
3. Убедитесь, что данные в файле содержат транзакции за выбранный период

## 📊 Проблемы с отчетами

### Отчеты не сохраняются

**Проблема:** Отчеты не создаются в папке `reports/`

**Решение:**
1. Проверьте права доступа к папке `reports/`:
   ```bash
   ls -la reports/
   ```
2. Создайте папку, если её нет:
   ```bash
   mkdir -p reports
   ```
3. Проверьте логи: `tail -f logs/reports.log`

### Ошибки при генерации отчетов

**Проблема:** Ошибки при вызове функций отчетов

**Решение:**
1. Убедитесь, что DataFrame не пустой
2. Проверьте, что категория существует в данных
3. Проверьте формат даты: `YYYY-MM-DD`
4. Проверьте логи: `tail -f logs/reports.log`

## 🧪 Проблемы с тестами

### Тесты не проходят

**Проблема:** `pytest` завершается с ошибками

**Решение:**
1. Убедитесь, что все зависимости установлены:
   ```bash
   poetry install
   ```
2. Запустите тесты с подробным выводом:
   ```bash
   poetry run pytest -v
   ```
3. Проверьте покрытие:
   ```bash
   poetry run pytest --cov=src --cov-report=term-missing
   ```

### Ошибки импорта в тестах

**Проблема:** `ModuleNotFoundError` в тестах

**Решение:**
1. Убедитесь, что вы находитесь в виртуальном окружении:
   ```bash
   poetry shell
   ```
2. Или используйте `poetry run`:
   ```bash
   poetry run pytest
   ```

## 📝 Проблемы с логированием

### Логи не создаются

**Проблема:** Файлы логов не создаются в папке `logs/`

**Решение:**
1. Создайте папку `logs/`:
   ```bash
   mkdir -p logs
   ```
2. Проверьте права доступа:
   ```bash
   chmod 755 logs
   ```
3. Проверьте, что приложение имеет права на запись

### Слишком много логов

**Проблема:** Файлы логов слишком большие

**Решение:**
1. Измените уровень логирования в `.env`:
   ```env
   LOG_LEVEL=WARNING
   ```
2. Регулярно очищайте старые логи:
   ```bash
   find logs/ -name "*.log" -mtime +30 -delete
   ```

## 🔐 Проблемы с безопасностью

### API ключи в логах

**Проблема:** API ключи видны в логах

**Решение:**
1. Приложение автоматически маскирует API ключи в логах
2. Убедитесь, что используете последнюю версию кода
3. Проверьте логи: `grep -i "api_key" logs/*.log` (не должно быть реальных ключей)

### Файл .env в Git

**Проблема:** Файл `.env` попал в репозиторий

**Решение:**
1. Удалите файл из Git (но не с диска):
   ```bash
   git rm --cached .env
   ```
2. Убедитесь, что `.env` в `.gitignore`
3. Смените все ключи, которые были в репозитории

## 📚 Дополнительная помощь

Если проблема не решена:

1. Проверьте [Issues](https://github.com/your-username/transaction-analyzer/issues) на GitHub
2. Создайте новый Issue с описанием проблемы
3. Приложите логи и информацию о системе:
   - Версия Python: `python --version`
   - Версия Poetry: `poetry --version`
   - Операционная система
   - Соответствующие логи из `logs/`

## 🔗 Полезные команды

### Проверка состояния

```bash
# Проверка версий
python --version
poetry --version

# Проверка установленных зависимостей
poetry show

# Проверка структуры проекта
tree -L 2 -I '__pycache__|*.pyc|.git'
```

### Очистка

```bash
# Очистка кеша Poetry
poetry cache clear pypi --all

# Очистка __pycache__
find . -type d -name __pycache__ -exec rm -r {} +
find . -type f -name "*.pyc" -delete

# Очистка старых логов
find logs/ -name "*.log" -mtime +7 -delete
```

### Отладка

```bash
# Запуск с отладкой
poetry run python -m pdb main.py

# Проверка импортов
poetry run python -c "import src.views; print('OK')"

# Проверка данных
poetry run python -c "from src.utils import load_transactions_from_excel; df = load_transactions_from_excel('data/operations.xlsx'); print(f'Загружено: {len(df)} транзакций')"
```

---

**Следующий шаг:** [Contributing](Contributing) - Как внести вклад в проект


