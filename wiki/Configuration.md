# Конфигурация

В этом разделе описаны все настройки конфигурации проекта Transaction Analyzer.

## 📁 Файлы конфигурации

Проект использует следующие файлы конфигурации:

- `.env` - Переменные окружения (не коммитится в Git)
- `.env.example` - Шаблон переменных окружения
- `user_settings.json` - Пользовательские настройки
- `pyproject.toml` - Конфигурация Poetry и инструментов разработки

## 🔐 Переменные окружения (.env)

### API настройки

```env
# API ключ для внешних сервисов (валюты, акции)
# Если не указан, используется бесплатный API exchangerate-api.com
API_KEY=your_api_key_here

# URL API для получения курсов валют
# По умолчанию: https://api.exchangerate-api.com/v4
API_URL=https://api.exchangerate-api.com/v4
```

**Примечание:** Если `API_KEY` не указан или неверный, приложение автоматически использует бесплатный API `exchangerate-api.com`, который не требует ключа.

### Настройки логирования

```env
# Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
```

### Настройки данных

```env
# Директория с данными
DATA_DIR=./data

# Путь к файлу с транзакциями
EXCEL_FILE=data/operations.xlsx

# Валюта по умолчанию
DEFAULT_CURRENCY=RUB
```

## 👤 Пользовательские настройки (user_settings.json)

Файл `user_settings.json` содержит настройки, специфичные для пользователя:

```json
{
  "user_currencies": ["USD", "EUR"],
  "user_stocks": ["AAPL", "AMZN", "GOOGL", "MSFT", "TSLA"]
}
```

### Параметры

#### user_currencies

Список валют для отображения курсов на веб-страницах.

**Доступные валюты:**
- `USD` - Доллар США
- `EUR` - Евро
- `GBP` - Британский фунт
- `JPY` - Японская йена
- `CNY` - Китайский юань
- И другие валюты, поддерживаемые API

**Пример:**
```json
{
  "user_currencies": ["USD", "EUR", "GBP"]
}
```

#### user_stocks

Список тикеров акций для отображения цен на веб-страницах.

**Популярные тикеры:**
- `AAPL` - Apple Inc.
- `AMZN` - Amazon.com Inc.
- `GOOGL` - Alphabet Inc. (Google)
- `MSFT` - Microsoft Corporation
- `TSLA` - Tesla Inc.
- И другие тикеры, поддерживаемые Alpha Vantage API

**Пример:**
```json
{
  "user_stocks": ["AAPL", "AMZN", "GOOGL"]
}
```

**Примечание:** Для получения цен акций требуется API ключ Alpha Vantage. Если ключ не указан, цены акций не будут отображаться.

## ⚙️ Конфигурация Poetry (pyproject.toml)

### Основные зависимости

```toml
[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"
pandas = "^2.0.0"
openpyxl = "^3.1.5"
python-dotenv = "^1.2.1"
flask = "^3.0.0"
```

### Зависимости для разработки

```toml
[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
pytest-cov = "^7.0.0"
black = "^23.0.0"
flake8 = "^6.0.0"
isort = "^5.12.0"
mypy = "^1.0.0"
pandas-stubs = ">=2.2.0,<2.3.0"
types-requests = "^2.32.4.20250913"
```

### Конфигурация инструментов

#### Black (форматирование кода)

```toml
[tool.black]
line-length = 119
exclude = '''
^\.git/
'''
```

#### Flake8 (линтер)

```toml
[tool.flake8]
max-line-length = 119
exclude = [".git", "__pycache__"]
```

#### isort (сортировка импортов)

```toml
[tool.isort]
line_length = 119
```

#### mypy (проверка типов)

```toml
[tool.mypy]
python_version = "3.9"
disallow_untyped_defs = true
warn_return_any = true
no_implicit_optional = true
exclude = '''
^venv/
'''
```

## 📂 Структура данных

### Формат Excel файла

Файл `data/operations.xlsx` должен содержать следующие колонки:

| Колонка | Название | Формат | Описание |
|---------|----------|--------|----------|
| A | Дата операции | `DD.MM.YYYY HH:MM:SS` | Дата и время транзакции |
| B | Дата платежа | `DD.MM.YYYY` | Дата платежа |
| C | Номер карты | `*XXXX` | Маскированный номер карты |
| D | Статус | `OK`, `FAILED` | Статус операции |
| E | Сумма операции | Число | Сумма в оригинальной валюте |
| F | Валюта операции | `RUB`, `USD` и т.д. | Валюта транзакции |
| G | Сумма платежа | Число | Сумма в валюте счета (отрицательная для расходов) |
| H | Валюта платежа | `RUB` | Валюта счета |
| I | Кэшбэк | Число | Размер кешбэка |
| J | Категория | Текст | Категория транзакции |
| K | MCC | Код | Код категории |
| L | Описание | Текст | Описание транзакции |
| M | Бонусы | Число | Бонусы включая кешбэк |
| N | Округление на инвесткопилку | Число | Сумма округления |
| O | Сумма операции с округлением | Число | Округленная сумма |

## 🔒 Безопасность

### Файлы, которые НЕ должны коммититься в Git

Следующие файлы автоматически игнорируются Git (указаны в `.gitignore`):

- `.env` - Содержит секретные ключи
- `data/operations.xlsx` - Содержит персональные данные транзакций
- `logs/*.log` - Логи приложения
- `*.pyc`, `__pycache__/` - Скомпилированные файлы Python
- `.venv/`, `venv/` - Виртуальные окружения

### Рекомендации по безопасности

1. **Никогда не коммитьте `.env` файл** - он содержит секретные ключи
2. **Не добавляйте `data/operations.xlsx` в Git** - файл содержит персональные данные
3. **Используйте `.env.example`** как шаблон для других разработчиков
4. **Регулярно обновляйте зависимости** для получения исправлений безопасности

## 📝 Примеры конфигурации

### Минимальная конфигурация

Для работы без внешних API:

```env
# .env
LOG_LEVEL=INFO
EXCEL_FILE=data/operations.xlsx
```

```json
// user_settings.json
{
  "user_currencies": [],
  "user_stocks": []
}
```

### Полная конфигурация

Для работы со всеми функциями:

```env
# .env
API_KEY=your_alpha_vantage_api_key
API_URL=https://api.exchangerate-api.com/v4
LOG_LEVEL=INFO
EXCEL_FILE=data/operations.xlsx
DEFAULT_CURRENCY=RUB
```

```json
// user_settings.json
{
  "user_currencies": ["USD", "EUR", "GBP"],
  "user_stocks": ["AAPL", "AMZN", "GOOGL", "MSFT", "TSLA"]
}
```

## 🔄 Обновление конфигурации

После изменения конфигурации:

1. **Переменные окружения** - перезапустите приложение
2. **user_settings.json** - изменения применяются сразу при следующем запросе
3. **pyproject.toml** - выполните `poetry install` для обновления зависимостей

## 📚 Дополнительная информация

- [Установка](Installation) - Как установить проект
- [Использование](Usage) - Как использовать приложение
- [Решение проблем](Troubleshooting) - Решение проблем с конфигурацией

---

**Следующий шаг:** [Использование](Usage)



